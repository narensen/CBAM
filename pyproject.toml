[project]
name = "cbam"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Naren Sengodan", email = "scartegy@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "torch>=2.14.0",
]

[project.scripts]
cbam = "cbam:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
